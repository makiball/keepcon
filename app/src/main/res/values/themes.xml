<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.KEEPCON" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/keepcon_orange</item>
        <item name="colorPrimaryVariant">@color/keepcon_orange</item>
        <item name="colorOnPrimary">@color/keepcon_white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/keepcon_yellow</item>
        <item name="colorSecondaryVariant">@color/keepcon_light_yellow</item>
        <item name="colorOnSecondary">@color/keepcon_black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <!-- Customize your theme here. -->
    </style>
</resources>